# ============================================================================
# LIST ALL BUSINESSES
# ============================================================================

query ListAllBusinesses @auth(level: USER) {
  businesses {
    id
    name
    tier
    subscriptionStartDate
    subscriptionEndDate
    isActive
    maxUsers
    maxLocations
    logo
    banner
    phone
    createdAt
    updatedAt
  }
}
