# ============================================================================
# LIST ALL USERS
# ============================================================================

query ListAllUsers @auth(level: USER) {
  users {
    id
    businessId
    email
    firstName
    lastName
    mobile
    role
    isActive
    lastLoginAt
    createdAt
    updatedAt
  }
}
