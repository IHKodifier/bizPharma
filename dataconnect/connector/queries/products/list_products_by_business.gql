# ============================================================================
# LIST PRODUCTS BY BUSINESS
# ============================================================================

query ListProductsByBusiness($businessId: UUID!) @auth(level: USER_ANON, insecureReason: "Trial users (anonymous) need access to this functionality during their 4-week trial period. Business-scoped access is enforced via businessId validation.") {
  products(where: { businessId: { eq: $businessId } }) {
    id
    businessId
    genericName
    brandName
    nationalDrugCode
    barcode
    upc
    internalSKU
    dosageForm
    strength
    unit
    routeOfAdministration
    drugSchedule
    requiresPrescription
    category {
      id
      name
    }

    isActive
    isDiscontinued
    createdAt
  }
}
