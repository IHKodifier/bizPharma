# ============================================================================
# GET LOCATION BY ID
# ============================================================================

query GetLocationById($id: UUID!) @auth(level: USER_ANON, insecureReason: "Trial users (anonymous) need access to this functionality during their 4-week trial period. Business-scoped access is enforced via businessId validation.") {
  location(id: $id) {
    id
    businessId
    name
    code
    type
    parentId
    phone
    email
    operatingHours
    licenseNumber
    isActive
    createdAt
    updatedAt
    address {
      id
      line1
      line2
      city
      state
      postalCode
      country
      phone
    }
  }
}
