# ============================================================================
# DELETE LOCATION
# ============================================================================

mutation DeleteLocation($id: UUID!) @auth(level: PUBLIC) { # TODO: SECURITY: Revert to @auth(level: USER) before production
  location_delete(id: $id)
}
